{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "Certainly, let's focus on the user histories without including the code. Below are the user histories for creating and updating users, and creating and updating tasks, along with authentication using JWT.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.9.12"
  }
}
